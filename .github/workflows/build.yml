on: push
jobs:
  build-node:
    runs-on: ubuntu-latest
    container: node:18
    steps:
      - uses: actions/checkout@v3
      - run: |
          cd socket
          npm install
          node --version
          pwd
          node index.js
          node express.js  